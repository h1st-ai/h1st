This subfolder host the main H1st tutorial, which is served online at https://h1st.ai/.

Building requires jupyter-book `pip install jupyter-book`.

Build the tutorial HTML using `cd notebooks; jupyter-book .`
